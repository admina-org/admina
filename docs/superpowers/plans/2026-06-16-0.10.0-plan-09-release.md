# 0.10.0 Plan 09 — Release Preparation (docs honesty, CHANGELOG, version bump — NO TAG)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Finish the docs-honesty residual, fix the stale scaffold template, assemble the real `CHANGELOG.md [0.10.0]` from the running notes, and bump the version to 0.10.0 across all tracked manifests + lockfiles — then STOP before any git tag (the maintainer authorizes the tag explicitly; tags trigger PyPI/ghcr publication).

**Architecture:** Editorial + mechanical. The version bump touches the 7 files `scripts/check-versions.py` enforces; the `[rust]` pin widens to a range that stays resolvable (the 0.10 core is not on PyPI until the tag). The CHANGELOG is assembled from `docs/superpowers/0.10.0-changelog-notes.md` (the running per-plan notes), reorganized by Keep-a-Changelog change-type and written user-facing — excluding the deferred items (Presidio) and the carry-forward section.

**Tech Stack:** Python 3.11, pytest, `uv lock`, optionally `cargo`. Branch `release/0.10.0`. Commit per task; **NEVER tag; do not push.** Task 5 surfaces the exact tag commands for the maintainer.

**Scope note:** Presidio is DEFERRED (maintainer decision) — the CHANGELOG must NOT claim it. The `get_pii_engine("presidio")` error message references `admina-framework[presidio]` (a not-yet-existing extra) — adjust it to say presidio is not available in this release (see Task 1).

---

### Task 1: Docs-honesty residual (MODEL_CARD engines claim, pipeline-order, presidio message)

The architecture review found docs claims that no longer match the wired reality. Luhn was already fixed (Plan 06). Close the rest.

**Files:** `MODEL_CARD.md`, the pipeline-order docs (`docs/guides/architecture.md` + any README pipeline diagram), `admina/engines/__init__.py` (the presidio error message); no code-behavior change.

- [ ] **Step 1 — MODEL_CARD "behaviorally equivalent engines":** find the claim (`grep -n "equivalent\|behavioral\|parity" MODEL_CARD.md`). The Rust and Python engines are NOT behaviorally equivalent (the repo's own `test_firewall_parity.py` pins a 7/14-vs-14/14 firewall evasion gap, and Rust PII lacks EU IDs/NER + Luhn). Rewrite the claim to state the measured difference honestly, consistent with the README's existing 7/14 disclosure. Neutral-technical wording.
- [ ] **Step 2 — pipeline order:** the canonical order is **loop → firewall → PII → guards** (`admina/domains/governance.py run_pipeline`). Find any doc showing a different order (`grep -rn "firewall.*loop\|loop.*firewall\|pipeline\|order" docs/guides/architecture.md README.md | head`). Align the documented order to the canonical one. (After Plan 03+03b, ALL surfaces use this order — the docs should reflect it.)
- [ ] **Step 3 — presidio error message:** in `admina/engines/__init__.py` `get_pii_engine`, the unknown-engine `ValueError` message references `'presidio' requires admina-framework[presidio] (0.10+)`. Since presidio is deferred (no `[presidio]` extra ships in 0.10), change the message to: `Unknown pii_engine {name!r}. Available: {sorted(_PII_ENGINE_FACTORIES)}. (A Presidio-backed engine is planned for a later release.)` — so the error doesn't point at a non-existent extra. Keep the test that asserts ValueError for an unknown name (update the match string if it pins the old text).
- [ ] **Step 4:** run the affected tests (`.venv/bin/python -m pytest tests/ -k "pii_engine or engines or model_card" -q` + any doc test) → green. Full suite no regressions.
- [ ] **Step 5:** changelog-notes (`## Plan 09 — release` → `### Documentation`): `- Corrected MODEL_CARD's engine-equivalence claim (Rust vs Python firewall/PII differ — measured, not equivalent) and aligned the documented governance pipeline order (loop → firewall → PII → guards) across the guides.`
- [ ] **Step 6: Commit** `docs: correct engine-equivalence claim and pipeline-order documentation`

---

### Task 2: Fix the stale docker-compose scaffold template

`admina/cli/templates/docker-compose.yml.j2` hardcodes `ghcr.io/admina-org/admina-proxy:0.9.0` and `admina-dashboard:0.9.0` (lines 12, 43) — stale (framework is moving to 0.10.0). `admina init` scaffolds these for users.

**Files:** `admina/cli/templates/docker-compose.yml.j2`; `admina/cli/main.py` (`_scaffold_project` render context); Test the scaffold output.

- [ ] **Step 1:** decide the approach: template the image tags from the framework version (cleanest — no future drift). In `admina/cli/main.py` `_scaffold_project` (find it; it renders the templates), inject the version into the render context: `"admina_version": __version__` (the file already imports `__version__` for the plugin scaffold per Plan 01 Task 3 — reuse it). In `docker-compose.yml.j2`, change the two image tags to `ghcr.io/admina-org/admina-proxy:{{ admina_version }}` and `admina-dashboard:{{ admina_version }}`.
- [ ] **Step 2 (test):** if there's an existing scaffold test (`grep -rln "docker-compose\|_scaffold_project\|admina init" tests/`), add/extend it to assert the rendered docker-compose contains the current `__version__` (not `0.9.0`). Mirror the existing scaffold-test style.
- [ ] **Step 3:** changelog-notes (`### Fixed`): `- 'admina init' now scaffolds docker-compose image tags from the framework version instead of a hardcoded stale 0.9.0.`
- [ ] **Step 4: Commit** `fix(cli): scaffold docker-compose image tags from the framework version`

---

### Task 3: Assemble CHANGELOG.md [0.10.0] from the running notes

**Files:** `CHANGELOG.md`; source: `docs/superpowers/0.10.0-changelog-notes.md`.

- [ ] **Step 1:** read the WHOLE `docs/superpowers/0.10.0-changelog-notes.md` (183 lines, organized per-plan) AND skim the commit log `git log --oneline main..HEAD` to catch anything. Assemble a single `## [0.10.0] — <DATE>` section in `CHANGELOG.md`, inserted between `## [Unreleased]` (keep it, empty) and `## [0.9.5]`. Use the date placeholder `YYYY-MM-DD` (the release.yml regex matches `## [0.10.0]` regardless of the date suffix; the maintainer sets the real date at tag time, or use today's date).

- [ ] **Step 2 — reorganize by Keep-a-Changelog change-type** (NOT by plan). Sanctioned headings (from prior releases): `Added`, `Changed`, `Fixed`, `Security`, `Removed`, `Documentation`, `Internal`. Write USER-FACING entries (a user reading the changelog, not an internal plan reader). RULES:
  - **Do NOT mention** internal plan structure (Phase 0, D1, D2, D6, "Plan 07"), the `docs/superpowers/` artifacts, or the carry-forward section.
  - **Do NOT claim Presidio** (deferred). Do NOT claim streaming (deferred to 0.11).
  - **Tone:** neutral-technical, describe what the code does for the user; no marketing, no self-judgement of prior code (the project's commit-tone rule).
  - **Highlight the user-visible headline items:** five new model adapters (Anthropic, Mistral, Bedrock, Gemini, vLLM) + per-provider/`adapters`/`all` extras [Added]; configurable RetryPolicy on the governed primitives [Added]; `ADMINA_ENGINE` selection + unified engine acquisition [Added/Changed]; `GovernedModel` now runs full governance (firewall + guards) by default — **clearly marked as a behavior change** [Changed]; the forensic chain-integrity hardening [Fixed/Security]; the auth fixes — WS cookie, API-key provider cookie, **fail-open default closed**, WS Origin allow-list [Security/Fixed]; the compliance-score, Luhn, PII-overlap, schema_version, OISG-S2 fixes [Fixed]; the dict-key PII scanning + dict-result MCP PII redaction [Fixed].
  - Group related fixes into readable bullets; don't transcribe every commit one-to-one, but don't drop a user-visible change.

- [ ] **Step 3:** verify the section heading is exactly `## [0.10.0] — <date>` (the `release.yml` extractor regex `## \[0.10.0\].*?` keys on `## [0.10.0]`). Confirm `## [Unreleased]` remains above it (empty is fine).
- [ ] **Step 4:** self-review the assembled section: no plan/internal refs, no Presidio/streaming claims, behavior change (GovernedModel default protection) clearly flagged, neutral tone, all headline items present. Fix inline.
- [ ] **Step 5: Commit** `docs: assemble CHANGELOG for 0.10.0`

---

### Task 4: Version bump to 0.10.0 (7 manifests + lockfiles + [rust] pin)

**Files:** `pyproject.toml:22`, `admina/__init__.py:26`, `core-rust/pyproject.toml:22`, `core-rust/Cargo.toml:18`, regenerate `uv.lock` + `core-rust/Cargo.lock` + `core-rust/uv.lock`; widen the `[rust]` pin.

- [ ] **Step 1:** set `version = "0.10.0"` in `pyproject.toml`, `admina/__init__.py` (`__version__`), `core-rust/pyproject.toml`, `core-rust/Cargo.toml` (`[package] version`).
- [ ] **Step 2 — widen the `[rust]` pin** (pyproject.toml line ~121) from `admina-core>=0.9.3,<0.10` to a range that STAYS RESOLVABLE before the 0.10 core is published: `admina-core>=0.9.3,<0.11`. (Keep it a RANGE, never `==` — a strict pin creates a release chicken-and-egg: the 0.10.0 core is published by the SAME tag and is not on PyPI until then. The range lets `uv lock` resolve to the already-published 0.9.5 now and pick up 0.10.0 at release; the engine-bridge ABI is stable across the line.) Update the long comment block to say the ABI is stable across the 0.9.x–0.10.x line.
- [ ] **Step 3 — regenerate lockfiles:**
  - `uv lock` (root) — regenerates `uv.lock` (admina-framework entry → 0.10.0). MEMORY gotcha: regenerate by hand after version/dep changes. If it fails on the [rust] extra, report the exact error.
  - `core-rust`: regenerate `Cargo.lock` (`cd core-rust && cargo update -p admina_core` — needs cargo; if cargo is unavailable, hand-edit the `admina_core` version entry in `core-rust/Cargo.lock` to 0.10.0 and REPORT that cargo was unavailable) and `core-rust/uv.lock` (`uv lock` inside core-rust, or hand-edit the admina-core entry to 0.10.0).
- [ ] **Step 4:** run `python3 scripts/check-versions.py` → MUST print "All versions aligned." If it reports a mismatch, fix the named file until aligned (this is the gate that guards the release).
- [ ] **Step 5:** `.venv/bin/python -m pytest tests/ -m "not benchmark" -q --tb=short 2>&1 | tail -3` → no regressions; `.venv/bin/python -c "import admina; print(admina.__version__)"` → `0.10.0`.
- [ ] **Step 6: Commit** `chore: release 0.10.0 — version bump and lockfile alignment`

---

### Task 5: Final verification + tag handoff (NO TAG — maintainer authorizes)

- [ ] Full suite green; `ruff check . && ruff format --check .` clean; `python3 scripts/check-versions.py` → aligned; `grep -rn "0.9.5\|0.9.0" pyproject.toml admina/__init__.py core-rust/ admina/cli/templates/docker-compose.yml.j2` shows NO stale framework versions left (clickhouse/grafana/etc. third-party image tags are fine).
- [ ] Confirm CHANGELOG `## [0.10.0]` section is present and well-formed; `git log --oneline main..HEAD | wc -l` (report the commit count).
- [ ] **DO NOT TAG.** Produce a handoff block for the maintainer with the EXACT commands and the publication consequences:
  - the current branch/HEAD SHA, and that `release/0.10.0` should be merged to `main` (or the tag cut from this branch per the project's release flow — state which, based on how prior releases were tagged: `git tag --list` + `git log v0.9.5` to see the pattern).
  - the exact `git tag -a v0.10.0 -m "..."` + `git push origin v0.10.0` commands.
  - which workflows the tag fires (`release.yml` → PyPI admina-framework via Trusted Publisher + GitHub Release; `release-core.yml` → PyPI admina-core; `release-docker.yml` → ghcr.io images) and that **PyPI is delete-restricted** (a mistaken 0.10.0 burns the version — must bump to 0.10.1).
  - the reminder that per the maintainer's own rule, the tag requires their explicit "tag it" / "vai".
- [ ] **Commit** any final doc/handoff note if you created one (e.g. append the handoff to the changelog-notes or a `RELEASE-0.10.0.md`), OR just report the handoff in your final message. Do NOT create a tag.

---

## Self-review notes

- **No tag, no push** — the version bump and CHANGELOG are committed to the branch, but tagging (which triggers irreversible PyPI/ghcr publication) is the maintainer's explicit call. Task 5 hands off the exact commands + consequences.
- **[rust] pin stays a resolvable range** (`>=0.9.3,<0.11`) to avoid the publish chicken-and-egg; the core's OWN version bumps to 0.10.0 (check-versions enforces that), but the framework's dependency constraint is a range satisfiable by the currently-published core.
- **CHANGELOG honesty** — describes only what shipped (no Presidio, no streaming), flags the GovernedModel default-protection behavior change, neutral-technical tone, no internal plan/artifact references.
- **check-versions.py is the release gate** — it must print "All versions aligned" before the handoff; if cargo is unavailable for `Cargo.lock`, hand-edit and re-run the check.
- The deferred items (Presidio, streaming, the remaining medium/low audit findings, the Rust-parity work, the signed-forensic-state-file follow-up) stay tracked in the changelog-notes carry-forward section — NOT in the public CHANGELOG.
